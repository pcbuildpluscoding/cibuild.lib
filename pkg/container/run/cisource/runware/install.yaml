---
modules: [utility.module]
schedule: [editCiRunware]
editCiRunware:
  baseDir: $CI_BASE_DIR/runware
  inputFile: [cischedule.yaml, finalCleanup.yaml, init.yaml, jobspec-task1.yaml]
  parameter:
    - findWith: <jobId>
      replaceWith: $CI_JOB_ID